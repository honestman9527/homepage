---
import "../styles/global.css";
import { readTheme } from "@/lib/theme";
import {
  applyInitialSidebarPreference,
  readSidebarPreference,
} from "@/lib/sidebar-preference";
import type { SiteConfig } from "@/lib/site-config";
import type { Language } from "@/i18n/ui";
interface Props {
  title: string; description: string; lang: Language; skipLabel: string;
  favicon: SiteConfig["favicon"];
  alternates?: { lang: Language; href: string }[];
  article?: { published: Date; updated?: Date };
}
const { title, description, lang, skipLabel, favicon, alternates = [], article } = Astro.props;
const canonical = new URL(Astro.url.pathname.replace(/\/+$/, "") || "/", Astro.site);
const themeScript = `document.documentElement.classList.toggle("dark", (${readTheme.toString()})().theme === "dark");`;
const sidebarScript = `(${applyInitialSidebarPreference.toString()})(${readSidebarPreference.toString()});`;
---
<!doctype html>
<html lang={lang}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <meta name="generator" content={Astro.generator} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content={article ? "article" : "website"} />
    <meta property="og:url" content={canonical} />
    {article && <meta property="article:published_time" content={article.published.toISOString()} />}
    {article?.updated && <meta property="article:modified_time" content={article.updated.toISOString()} />}
    <link rel="canonical" href={canonical} />
    {alternates.map((alternate) => <link rel="alternate" hreflang={alternate.lang} href={new URL(alternate.href, Astro.site)} />)}
    <link rel="icon" href={favicon.light} media="(prefers-color-scheme: light)" />
    <link rel="icon" href={favicon.dark} media="(prefers-color-scheme: dark)" />
    <script is:inline set:html={themeScript} />
    <script is:inline set:html={sidebarScript} />
    <title>{title}</title>
  </head>
  <body>
    <a href="#main-content" class="skip-link">{skipLabel}</a>
    <slot />
  </body>
</html>
